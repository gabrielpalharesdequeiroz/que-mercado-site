
const id=new URLSearchParams(location.search).get('id');const p=JSON.parse(localStorage.getItem('qm_posts')||'[]').find(x=>x.id===id);const root=document.getElementById('dynamicPost');if(!p){root.querySelector('h1').textContent='Artigo não encontrado';root.querySelector('.article-content').innerHTML='<p>Este conteúdo pode ter sido removido do protótipo local.</p>'}else{document.title=p.title+' | Que Mercado';root.querySelector('.article-cat').textContent=p.category||'BLOG';root.querySelector('h1').textContent=p.title;root.querySelector('.article-content').innerHTML=p.content.split(/\n+/).filter(Boolean).map(t=>`<p>${t}</p>`).join('')}
